import styles from './Certificates.module.scss';

const Certificates = () => {
  return (
    <section className={styles.certificates}>
      <div className={styles.certificates__inner}>
        <h2 className={styles.contacts__heading}>- Сертифікати -</h2>
      </div>
    </section>
  );
};

export default Certificates;
